<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.placementstaff">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PosterChangeActivity"
            android:parentActivityName=".HomePage"></activity>
        <activity
            android:name=".ApplicationDetailsActivity"
            android:parentActivityName=".ApplicationActivity" />
        <activity android:name=".ApplicationActivity" />
        <activity android:name=".ViewApplicationDetails" />
        <activity android:name=".ViewPlacementDetails" />
        <activity
            android:name=".AddPlacementOffer"
            android:parentActivityName=".PlacementPage" />
        <activity android:name=".StudentsPage" />
        <activity
            android:name=".ApplicationPage"
            android:parentActivityName=".ApplicationActivity" />
        <activity android:name=".PlacementPage" />
        <activity android:name=".HomePage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>